<div class="card bg-secondary py-5 mb-3 bg-secondary">
  <div class="container-fluid">
    <div class="panel">
      <%= search_form_for @citizens.search, url: citizens_path do |f| %>
        <div class="row">
          <div class="form-group col-md-6">
            <%= f.label :name_cont %>
            <%= f.search_field :name_cont, class: 'form-control form-control-alternative' %>
          </div>
          <div class="form-group col-md-6">
            <%= f.label :email_cont %>
            <%= f.search_field :email_cont, class: 'form-control form-control-alternative' %>
          </div>
          <div class="form-group col-md-6">
            <%= f.label :cpf_eq %>
            <%= f.search_field :cpf_eq, class: 'form-control form-control-alternative' %>
          </div>
          <div class="form-group col-md-6">
            <%= f.label :cns_cont %>
            <%= f.search_field :cns_cont, class: 'form-control form-control-alternative' %>
          </div>
          <div class="col-md-12 text-right">
            <%= f.submit 'Pesquisar', class: 'btn btn-outline-info btn-round' %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
